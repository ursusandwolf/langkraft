ktor {
    deployment {
        port = 8080
        host = "0.0.0.0"
    }
    application {
        modules = [ com.langkraft.backend.ApplicationKt.module ]
    }
}

jwt {
    secret = "langkraft-super-secret-key-change-in-production"
    issuer = "http://0.0.0.0:8080/"
    audience = "http://0.0.0.0:8080/api"
    realm = "Access to 'api'"
}
